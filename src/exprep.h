#include <stdio.h>
#include <cmath>
#include <vector>

void doptpoisson(int* ndim, double* acc, double* dnear, double* dfar, int* nt, double* pp, double* wei);
void doptpoisson2(int* ndim, double* acc, double* dnear, double* dfar, int* nt, double* pp, double* wei);


